# Database Folder

This folder stores the SQLite database (`memory.db`) generated automatically when the application is first run.

The database file is not included in this repository because it is created locally at runtime.